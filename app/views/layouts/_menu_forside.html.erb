<%#Her skal jeg finde menupunkterne til hovedmenuen i Contents. Dvs. alle dem der tilhører Controlleren pages og som er aktive. Menuerne dannes herfra, men det efterfølgende indhold hentes i Pages eller anden tabel.%>
<ul>
	
	
<% @last_one = @main_menu.last.id %>

<% @main_menu.each do |menu| -%>

	<% if menu.redirect? %>
	<% @controller_name = menu.controller_redirect %>
	<% @action_name = menu.action_redirect %>
	<% else %>
	<% @controller_name = menu.controller_name %>
	<% @action_name = 'show' %>
	<% end %>

	<% if @main_menu[1] == nil %>
	<li class="border_left">
	<%= link_to menu.navlabel.mb_chars.upcase, :controller => @controller_name, :action => @action_name, :id => menu.resource_id %></li>
	
	<%else%>

		<% if @action_name == 'forside'%>
			<li class="border_left"><%= link_to menu.navlabel.mb_chars.upcase, root_url %></li>
		<% else %>
			<% unless @last_one == menu.id %>
			<li>
			<%= link_to menu.navlabel.mb_chars.upcase, :controller => @controller_name, :action => @action_name, :id => menu.resource_id %>
			</li>
			<% end %>
		<%end %>
	
	
	
		<% if @last_one == menu.id%>
			<li>
			<%= link_to menu.navlabel.mb_chars.upcase, :controller => @controller_name, :action => @action_name, :id => menu.resource_id %>
			</li>
		
		<% end %>
		
	<% end %>
	
<% end %>

</ul>


