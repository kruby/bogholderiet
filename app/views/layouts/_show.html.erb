<!-- Husk at partials benytter "_menu.html.erb" underscore! -->

	<%= textilize(@headline)%>
	<%= textilize(@content)%>
		
		<%# if @image == 'no_image' %>
		<%# else %>
		<%#= image_tag(@image, :class => @image_class) %>
		<%# end %>